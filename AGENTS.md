# Project guidance

This project is a local web app for a shopping cart flow. Keep the app self-contained and avoid adding tool-specific deployment hooks or editor metadata.

Use the project structure under `src/` for pages, components, and cart logic. Prefer simple, maintainable React code and keep styling consistent with the existing design system.

